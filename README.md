to run project.

npm install 
npm run dev

php artisan serve

set databse credential in env file.

run migrations by,  php artisan migrate
run seeder by, php artisan db:seed --class=LeaderboardSeeder

check url now, 
http://127.0.0.1:8000/leaderboard?filter=all

